declare function R_PointToDist2(
    x: fixed_t,
    y: fixed_t,
    dest_x: fixed_t,
    dest_y: fixed_t
): fixed_t;

declare function R_PointToAngle2(
    x: fixed_t,
    y: fixed_t,
    dest_x: fixed_t,
    dest_y: fixed_t
): angle_t;
